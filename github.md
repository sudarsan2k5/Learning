1. Delete a local branch
`git branch -d branch_name`
2. Delete the remote branch:- `git push origin --delete your-branch-name`
3. one-liner :- `git branch -d your-branch-name && git push origin --delete your-branch-name`
